#include <stdio.h>

int main (void) {
    int number1;
    int number2;
    int total;
    
    printf("Please enter two integers:    ");
    scanf("%d %d", &number1, &number2);
    total = number1 + number2;
    printf("%d + %d = %d\n", number1 ,number2, total);
    
    return 0;
}
