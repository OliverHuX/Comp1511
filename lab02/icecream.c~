#include <stdio.h>

//define how much money does Matilda have
#define PROPERTY_VALUE 10

int main (void) {
    int scoops;
    int price;
    
    //ask Matilda how many scoops do you want
    printf("How many scoops? ");
    scanf("%d", &scoops);
    //what is the icecream price
    printf("How many dollars does each scoop cost? ");
    scanf("%d", &price);
    //check whether she has enough money
    if (PROPERTY_VALUE >= price*scoops) {
    printf("You have enough money!\n");
    } else {
    printf("Oh no, you don't have enough money :(\n");
      }

    return 0;
}
    
    
     
