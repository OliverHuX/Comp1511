#include <stdio.h>

int main (void) {
    int number;
    int endPoint = 1;
    
    printf("Enter number: ");
    scanf("%d", &number);
    
    while (number > endPoint) {
    
    if (endPoint % 3 == 0 || endPoint % 5 == 0) {
    printf("%d\n", endPoint);
    }
    
    endPoint++;
    
    }
    
    return 0;
}

