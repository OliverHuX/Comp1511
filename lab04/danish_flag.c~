#include <stdio.h>

int main (void) {
    int size; 
    
    printf("Enter the flag size: ");
    scanf("%d", &size);
    
    int high = 2*size;
    int wide = 3*size;
        
    int counter = 0;
    int position = 1;
    int bound = 1;
    while (counter < high*2) {
    
    while (position <= wide*3) {
    if (bound == high || bound == high+1) {
    printf(" ");
    } else if (position <= wide-1 || position > wide+1) {
    printf("#");
    } else {printf(" ");}
    position++;
    }
    position = 1;
    bound++;
    
    
    
    
    
    
    
    printf("\n");
    counter++;
    }
    return 0;
    }
    
    
    
    
